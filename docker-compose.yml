version: "3"

networks:
  corp:
    driver: bridge

services:
  web:
    container_name: web
    networks:
      - corp
    build: .
    volumes:
      - ./bazel-cache:/data/www/bazel-cache
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "8080:80"